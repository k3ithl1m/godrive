const INITIAL_STATE = {
  email: "",
  name: "",
  go: "",
  drive:"",
  description: "",
};

export default (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
}
